node_modules

# Ignore lockfiles we don't use (Bun uses bun.lock — commit it for reproducible installs)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

.DS_Store
.cache
.env

.data
.vercel
.output
.wrangler
.netlify
dist
/build/
/api/
/server/build
/public/build

.tanstack